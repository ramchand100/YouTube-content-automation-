# Same dependencies as the root requirements.txt. Kept here only so
# `pip install -r tools/requirements.txt` also works from within tools/ —
# do not duplicate the dependency list here, it will drift out of sync.
-r ../requirements.txt

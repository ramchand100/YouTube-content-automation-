{
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "cat PRODUCTION_STATUS.md 2>/dev/null | jq -Rs '{hookSpecificOutput: {hookEventName: \"SessionStart\", additionalContext: (\"=== PRODUCTION STATUS (auto-loaded at session start; see PRODUCTION_STATUS.md) ===\\n\" + .)}}'",
            "timeout": 10
          }
        ]
      }
    ]
  }
}
